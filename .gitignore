Makefile
*.o
.deps
.dirstamp
src/cap-fmu
Makefile.in
aclocal.m4
ar-lib
autom4te.cache/
compile
config.*
configure
depcomp
install-sh
libtool
ltmain.sh
m4/
missing
stamp-h1

# Editor / autotools backup files (config.guess~, config.sub~, configure~, ...)
*~

# Test harness (automake) artefacts
test-driver
tests/fmu_test
tests/mav_io_test
tests/fss_test
*.trs
*.log

# Static-analysis artefacts (see check-code.sh)
compile_commands.json
clang-tidy.log

# Distribution tarball (make dist / make distcheck)
/cap-fmu-*.tar.gz
